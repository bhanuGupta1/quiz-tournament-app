[phases.setup]
nixPkgs = ['maven', 'openjdk17']

[phases.build]
cmds = ['mvn clean package -DskipTests']

[phases.start]
cmd = 'java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar target/quiz-backend-0.0.1-SNAPSHOT.jar'