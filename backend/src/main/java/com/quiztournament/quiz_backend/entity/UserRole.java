package com.quiztournament.quiz_backend.entity;

/**
 * Enum defining user roles in the Quiz Tournament system
 * ADMIN: Can create/manage tournaments and users
 * PLAYER: Can participate in quiz tournaments
 */
public enum UserRole {
    ADMIN,
    PLAYER
}